<UserControl x:Class="RemoteVisionConsole.Module.Views.CalculationMethodItemView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Border BorderThickness="1">
        <Border.Style>
            <Style TargetType="Border">
                <Setter Property="BorderBrush" Value="Black"/>
                <Style.Triggers>
                    <DataTrigger Binding="{Binding Errored}" Value="True">
                        <Setter Property="BorderBrush" Value="Red"/>
                    </DataTrigger>
                </Style.Triggers>
            </Style>
        </Border.Style>
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <TextBox Grid.Column="0" Width="60" Text="{Binding OutputName, UpdateSourceTrigger=PropertyChanged, Delay=400}" />
            <TextBlock Grid.Column="1" Text=" = " FontWeight="SemiBold" />
            <TextBox Grid.Column="2" Text="{Binding MethodDefinition, UpdateSourceTrigger=PropertyChanged, Delay=400}" />
        </Grid>
    </Border>
</UserControl>