<UserControl x:Class="RemoteVisionConsole.Module.Views.WeightItemView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid Width="150">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <TextBlock Grid.Column="0" Text="{Binding Name}" TextTrimming="CharacterEllipsis" TextAlignment="Center"/>
        <TextBox Grid.Column="1" Text="{Binding Weight, TargetNullValue=Uninitialized, Delay=400, UpdateSourceTrigger=PropertyChanged}"/>
    </Grid>
</UserControl>
